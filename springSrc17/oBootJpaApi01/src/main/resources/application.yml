server: 
  port: 8386
spring:
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521/xe
    username: scottJpa
    password: tiger
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update  
logging.level:
  org.hibernate.SQL: debug #logger을 통해 하이버네이트 실행 SQL